syntax = "proto3";
package api;

import "request.proto";
import "response.proto";

// Diamond dependency: api imports request and response, both import common
message RequestResponsePair {
    Request request = 1;
    Response response = 2;
}

service ApiService {
    rpc Call(Request) returns (Response);
}
