syntax = "proto3";
package model;

import "base.proto";
import "types.proto";

// Multiple direct dependencies: imports both base and types
message EnrichedModel {
    base.BaseMessage metadata = 1;
    types.User owner = 2;
    string description = 3;
}
