syntax = "proto3";
package types;

import "common.proto";

// Types that depend on common
message User {
    common.UUID id = 1;
    string name = 2;
    common.Timestamp created_at = 3;
}

message Product {
    common.UUID id = 1;
    string title = 2;
    double price = 3;
}
